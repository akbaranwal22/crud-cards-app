{
    "name": "json-server-backend",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
      "start": "json-server --watch ../mock/db.json --port 10000 --host 0.0.0.0",
      "build": "echo 'no build needed'"
    },
    "dependencies": {
      "json-server": "^0.17.3"
    }
  }
  